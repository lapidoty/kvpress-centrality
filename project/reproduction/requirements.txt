# Clean public stack for reproducing the CentralityPress benchmarks (NOT a pip-freeze of the authoring
# venv, which uses a Meta-internal torch build). Versions follow kvpress 0.5.4's declared ranges.
torch>=2.3.1,<3
transformers>=4.56.0,<5.3
flash-attn            # install with --no-build-isolation (see Dockerfile)
accelerate
datasets
numpy
pandas
pyyaml
fire
# analysis / figures
matplotlib
scipy
